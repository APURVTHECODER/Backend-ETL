apiVersion: v1
kind: Service
metadata:
  name: backend-service
spec:
  type: ClusterIP        # Internal-only service
  selector:
    app: backend         # Matches the Deployment’s pod label
  ports:
    - protocol: TCP
      port: 80           # Port that other services inside the cluster will use
      targetPort: 8000   # The containerPort your backend listens on